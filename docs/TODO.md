# TODO

## Сделано
- [x] Конфиг из линейного в структурированный — `project.ysak` v2 (секции `train`, `mlflow`, `augmentation`, `export`, `tools`, `postExports`), старые плоские файлы мигрируются автоматически. Профили экспорта и аугментаций теперь сохраняются в конфиг.
- [x] Аугментации: ключи snake_case передаются CLI-аргументами (раньше YAML с битыми ключами `hsvH`), профиль «Default (Ultralytics)» приведён к реальным дефолтам Ultralytics.
- [x] Проектная парадигма (2026-07-04): проект = файл `*.ysak` (корень проекта — его папка). Открыть/Создать/Recent в шапке, вкладки неактивны без проекта, `project.ysak` больше не создаётся неявно. MRU и язык — в `%APPDATA%\YSAK\settings.json` (`AppSettingsService`). Ассоциация `*.ysak` (HKCU) и jump list — `WindowsShellService`, кнопка регистрации в System Doctor.

## В работе / дальше
- [ ] Single-instance (опционально): сейчас каждый запуск — отдельное окно; при желании mutex + передача аргументов в существующий процесс.
- [ ] Локализация новых строк шапки/System Doctor (Open/Create/Recent, статус ассоциации).
- [ ] Аугментации: применить фильтры MinWidth/MinHeight (предфильтрация мелких bbox в labels) — поля уже в профиле, но при тренировке пока не используются.
- [ ] Инференс: поддержка сегментации (`seg`) в `InferenceService` (сейчас честная заглушка с предупреждением).
- [ ] Датасет: опциональный «виртуальный» сплит через train.txt/val.txt вместо физического перемещения файлов.
- [ ] UI: перевести оставшиеся жёсткие `Margin` в ресурсы `CompactTheme` (`PageMargin`/`ColumnMargin`/`SplitterMargin`).
- [ ] Локализация: полноценный RU/EN переключатель (сейчас `LocalizationService` покрывает только описания аугментаций).
